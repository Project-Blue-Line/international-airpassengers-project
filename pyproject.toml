[project]
name = "international-airpassengers-project"
version = "0.1.0"
description = "Capstone: forecast 12 months of international air passengers, start to finish"
requires-python = ">=3.11,<3.14"
dependencies = [
    # nixtlaverse — statsforecast brings utilsforecast + coreforecast with it
    "statsforecast>=2.0",
    # classical stats: STL, Ljung-Box, ACF, R-datasets
    "statsmodels>=0.14",
    "pandas>=2.2",
    "numpy>=2.0",
    # plotting (seaborn: course style in coursekit.plotting)
    "matplotlib>=3.9",
    "seaborn>=0.13",
]

# AutoGluon (extra credit only) is deliberately not a core dependency: it
# pulls torch (~2.5 GB). Install it in the notebook:
#     !pip install -q autogluon.timeseries

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["coursekit"]
